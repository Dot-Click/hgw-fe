export const APP_NAME = "HGW Legend Vault";
export const API_PREFIX = "/api";

export const ROLES = {
  USER: "user",
  ADMIN: "admin",
} as const;

export const SESSION_EXPIRY = 60 * 60 * 24 * 7; // 7 days
